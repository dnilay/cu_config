server:
  port: 8088
spring:
  boot:
    admin:
      client:
        url: http://localhost:8099
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dnilay/cu_config.git
